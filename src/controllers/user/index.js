const {
  registerController,
  loginController,
  logoutController,
  getCurrentController,
} = require("./authController");

module.exports = {
  registerController,
  loginController,
  logoutController,
  getCurrentController,
};
