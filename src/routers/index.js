const contactsRouter = require("./contactsRouter");
const authRouter = require("./authRouter");
const filesRouter = require("./filesRouter");

module.exports = { contactsRouter, authRouter, filesRouter };
